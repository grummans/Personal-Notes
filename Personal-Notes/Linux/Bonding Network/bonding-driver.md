### Bonding driver

- `bonding` is a module of Linux kerner.

- `bonding` create new interface network.

E.g `eth0 | eth1` -> `bond0` -> IP assign for `bond0`

-> Maybe request will work with `bond0`, doesn't need to know `eth0 | eth1` inside
