### What is VRRP?
- VRRP (Virtual Router Redundancy Protocol) is a network protocol is a network protocol.
- Features:
  -- Creating a virtual gateway.
  -- Sharing an unique IP among routers.
  -- Auto switching when incedents occur.
